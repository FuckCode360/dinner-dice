# AGENTS.md

## 角色

你在本仓库中作为 coding agent 工作。优先遵循 `docs/PROJECT.md`、`docs/PROGRESS.md`、`docs/USAGE_DEV.md` 和 `docs/PLANS/` 下的阶段计划。

## 项目规则

- 非简单修改前，先阅读项目文档，确认当前阶段目标和不做事项。
- 优先做小而可审查的改动，避免把第一版个人工具扩展成复杂平台。
- 不静默修改本地数据格式；如果必须改 `localStorage` key 或字段，需要同步更新文档和迁移说明。
- 不接入外卖平台登录、抓取、反爬绕过、第三方账号或云同步，除非用户明确进入新阶段。
- 保持仓库干净，不保留临时文件、死代码、无用途目录或未使用资源。

## 技术栈

- 前端：Vite + React + TypeScript。
- 样式：普通 CSS。
- 图标：`lucide-react`。
- 存储：浏览器 `localStorage`。

## 代码风格

- 组件和工具函数使用 TypeScript，优先保持简单直观。
- UI 以手机优先为基准，第一屏必须是可用工具界面，不做营销落地页。
- 业务逻辑尽量集中在小函数中，避免把筛选、摇号和存储规则散落到多个组件。
- 中文界面文案保持短句，避免大段说明文字。

## 受保护路径

除非任务明确需要，不要修改以下路径：

- `node_modules/`
- `dist/`
- `.vite/`
- 浏览器或构建工具生成的缓存目录

## 完成说明要求

每次任务结束时报告：

1. 修改了什么。
2. 为什么修改。
3. 如何测试。
4. 仍然存在的风险、限制或后续任务。
